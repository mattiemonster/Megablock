package mattiemonster.megablock.main;

// import com.badlogic.gdx.ApplicationAdapter;
import com.badlogic.gdx.Game;
import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.graphics.GL20;
import com.badlogic.gdx.graphics.Texture; // texture import
import com.badlogic.gdx.graphics.g2d.SpriteBatch; // sbatch import

import mattiemonster.megablock.main.states.play.PlayFullControl;

public class Megablock extends Game {
	// SpriteBatch batch;
	// Texture block; block def
	// Texture ErthalFire; fire def
	
	@Override
	public void create() {
		setScreen(new PlayFullControl(this));
		// batch = new SpriteBatch();
		// block = new Texture("DEV/RenderingTest/RenderingTest.png"); def
		// ErthalFire = new Texture("FIRETYPE/Erthal/ErthalFire.png"); def
	}

	@Override
	public void render() {
		Gdx.gl.glClearColor(0, 1, 1, 1);
		Gdx.gl.glClear(GL20.GL_COLOR_BUFFER_BIT);
		// batch.begin();
		//batch.draw(block, Gdx.graphics.getWidth() / 2, Gdx.graphics.getHeight() / 2); draw
		//batch.draw(ErthalFire, 0, 0); draw
		// batch.end();
	}
	
	@Override
	public void dispose() {
		// batch.dispose();
	}
}
